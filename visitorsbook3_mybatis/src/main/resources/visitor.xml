<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.visitor.visitorsbook.model.dao.VisitorDao">
	<insert id="registerVisitor" parameterType="com.visitor.visitorsbook.model.VisitorDto">
		insert into visitors (visitorid, visitorname, visitorpwd, email, joindate)
		values (#{visitorId}, #{visitorName}, #{visitorPwd}, #{email}, now())
	</insert>
	
	<select id="login" parameterType="map" resultType="com.visitor.visitorsbook.model.VisitorDto">
		select visitorid visitorId, visitorname visitorName
		from visitors
		where visitorid = #{visitorId} and visitorpwd = #{visitorPwd}
	</select>
</mapper>